function ExerciseCard({ title, description }) {

    return (

        <div className="exercise-card">

            <h2>{title}</h2>

            <p>{description}</p>

        </div>

    );

}

export default ExerciseCard;